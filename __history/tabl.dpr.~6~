program tabl;

{$APPTYPE CONSOLE}

{$R *.res}

uses
  System.SysUtils,
  Windows;

var i, k: uint8;


begin
setconsoleoutputCP(1251);
for I := 1 to 120 do
  if i mod 10 = 0 then
     writeln(i, #10)
  else
    write(i);

readln;
end.
