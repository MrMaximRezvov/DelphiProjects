program randtask8;

{$APPTYPE CONSOLE}

{$R *.res}

uses
  System.SysUtils;

var i, j, jold, count, istart, countbig: int32;




begin
repeat
i:= 0;
j:= 0;
istart:= 0;
count:= 0;
jold:= 0;

write('введите randseed: ');
readln(randseed);

for i := 1 to 10000 do
begin



j:= random(101);
if j>jold then
begin
inc(count);
istart:= i - count;
end;
if count>countbig then
countbig:= count;
count:=0;

end;



writeln('count=', countbig, ' start=', istart);
until false;
end.
