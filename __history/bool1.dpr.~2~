program bool1;

{$APPTYPE CONSOLE}

{$R *.res}

uses
  System.SysUtils;

var x, y: boolean;

begin
for x := false to true do
  for y := false to true do
    begin
      write(ord(x):2, ord(y):2);
    end;
readln;
end.
