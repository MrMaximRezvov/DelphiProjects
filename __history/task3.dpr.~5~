program task3;

{$APPTYPE CONSOLE}
{$R *.res}

uses
  System.SysUtils;

var
  a, b, c, d, x, y, z: int32;

begin
  repeat
    writeln('a, b, c, d');
    readln(a, b, c, d);
    if a > 0 then  if c > 0 then   if a > c then  x := a - c else x:= a+abs(c) else x:= abs(a)+abs(c);
    if b > 0 then  if d > 0 then   if b > d then  y := b - d else y:= b+abs(d) else y:= abs(b)+abs(d);
    writeln('x=', x, 'y=', y);
    while y <> 0 do
    begin
      z := y;
      y := x mod y;
      x := y;
    end;
    writeln('otvet=', abs(x)-1);
  until false;

end.
