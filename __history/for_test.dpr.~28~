program for_test;

{$APPTYPE CONSOLE}

{$R *.res}

uses
  System.SysUtils,
  windows;

var
  S, sum, sumc, i, nach, kon: int32;


const n1 = -1000;
const n2 = 10000;


begin

begin
  setconsoleoutputCP(1251);
sum:=0;
sumc:=0;
for i:= n1 to n2 do
if i mod 13 = 0 then
sum:=sum + i;
sum:=sumc+1;
writeln('sum= ', sum, #10);
writeln('sumc= ', sumc, #10);


end;
//writeln('Напишите начальное и конечное значение');
//readln(nach, kon);
// Нахожение суммы по формуле Гаусса
//S:= (nach+kon) * (kon - nach + 1) div 2;
//writeln('Формулой Гаусса= ', S);

readln
end.
