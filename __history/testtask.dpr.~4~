program testtask;

{$APPTYPE CONSOLE}
{$R *.res}

uses
  System.SysUtils;

var
  n, k, h, nas: int32;

label m;

begin
  readln(n, k);
m:
  readln(h);

  if k < h then
    nas := h - k;

  if h < k then
    nas := k - h;

  if nas < 0 then


  goto m;

end.
