program tablbool;

{$APPTYPE CONSOLE}
{$R *.res}

uses
  System.SysUtils;

var
  x, y: boolean;
  i, p: int16;
  result: ansistring;

begin

  for p := 0 to 15 do
  begin
    //result := '';
    for i := 0 to 3 do
    begin
      writeln;
      if p and (1 shl i) > 0 then
        //result := '1' + result
        write('1')
      else
        //result := '0' + result;
        write('0');
    end;
    //writeln;
    //writeln(result);
  end;
  readln;

end.
