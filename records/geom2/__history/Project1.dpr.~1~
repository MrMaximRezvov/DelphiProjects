program Project1;

{$APPTYPE CONSOLE}

{$R *.res}

uses
  System.SysUtils,
  u_trec in 'C:\Users\Maxim\Documents\Embarcadero\Studio\Projects\u_trec.pas';

var p1,p2,p3,L1,L2:trec;
    S1:trec;
    d1,d2:real;
    S2:string;
const s='Введите координаты точки на плоскости: ';
begin
  repeat
  p1.z:=5;
  write(s);
  readln(d1,d2);
  p1.init(d1,d2);
  //p1.x:=d1;//Error
  write(s);
  readln(d1,d2);
  p2.init(d1,d2);
 p2.show(2,'Координаты точки p2 ');
  p3:=p1+p2;// как вектора
  p3.show(2,'Суммарный вуктор (x,y) = '); //2 - число знаков после запятой
  p3:=p2-p1;
  p3.show(2,'Разностный вектор p2-p1 (x,y) = ');
  p3:=p1-p2;
  p3.show(2,'Разностный вектор p1-p2 (x,y) = ');
  d1:=p1.dist(p2);// self - содержит ссылку на память = ссылке переменной,
  // обратившейся к методу dist (синоним имени, вызвашей данный метод)
  writeln('Расстояние между точками p1,p2 => d1=',d1:0:2);
  d2:=p1.dist1(p2);
  writeln('Расстояние между точками dist1 p1,p2 => d2=',d2:0:2);
  L1.line(p1,p2);
  L1.show(2,'Уравнение линии (Line) : (k,b) = ');
  L2.line1(p2,p1);
  L2.show(2,'Уравнение линии (Line) : (k,b) = ');
  readln;
  // Работаем со строкой
{
  s1:='*=*';
  S2:= 3*s1;
  writeln(s2);
  readln;
  //
  write(s+' (3-ю точку) ');
  readln(d1,d2);
  p3.init(d1,d2);
  d1:=p1.square(p2,p3);
  writeln('Площадь треугольника = ',d1:0:2);
  d1:=p1.triangle_square(p2,p3);
  writeln('Площадь треугольника = ',d1:0:2);
  writeln(10*s1);
}
  readln;
  until false;
end.
