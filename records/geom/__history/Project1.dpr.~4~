program Project1;

{$APPTYPE CONSOLE}
{$R *.res}

uses
  System.SysUtils,
  Unit1 in 'Unit1.pas';

var
    p1, p2, p3, L1: tdot;
    d1, d2, d3, d4: real;

const s = 'Введите координаты точек на плоскости(x, y; x,y): ';

begin

      repeat
        write(s);
        readln(d1, d2, d3, d4);
        p1.init(d1, d2, d3, d4);
        //p1.print(3, s);
        p1.dist;
        readln;
      until false;

end.
