program project6;

{$APPTYPE CONSOLE}
{$R *.res}

uses
  System.SysUtils;

// Вывод 1-го элемента списка
procedure gip_triangle(a,b: real; var c: real);
begin
  c:=sqrt(a*a+b*b);
end;

function sum_triangle(a,b: real; var s: real):real;
begin
  s:=(a*b)/2;
  result := s;
end;

//
var a, b, c, s: real;
 cp : ^real;
begin
  repeat
    writeln('Введите a, b: ');
    readln(a);
    readln(b);
    gip_triangle(a, b, c);
    s := sum_triangle(a, b, s);
    writeln('Gip = ', c:0:3);
    writeln('Sum = ', s:0:3);
    readln
  until false;
  readln;
end.
