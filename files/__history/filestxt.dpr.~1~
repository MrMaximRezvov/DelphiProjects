program filestxt;

{$APPTYPE CONSOLE}

{$R *.res}

uses
  System.SysUtils;

var fin, fout: textfile;

begin
  assignfile(fin, './input.txt');
  assignfile(fout, './output.txt');
  rewrite(fin);
  rewrite(fout);
  closefile(fin);
  closefile(fout);
end.
